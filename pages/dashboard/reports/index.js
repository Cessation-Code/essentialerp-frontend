import React from 'react';
import withAuth from '../../../components/withAuth';

const Reports = () => {
    return (
        <div>
        <h1>Reports</h1>
        </div>
    );
}

export default withAuth(Reports);
